// Frontend configuration for HabitGuard
// API base URL — points to Express backend
export const API_BASE_URL =
  process.env.REACT_APP_API_URL || 'http://localhost:3000/api';

// Default user ID (replace with auth system later)
export const DEFAULT_USER_ID = 'user123';
